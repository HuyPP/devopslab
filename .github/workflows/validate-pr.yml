# validate-pr.yml đã được thay thế bởi pr-checks.yml
# File này giữ lại để tham khảo lịch sử, không có jobs nào chạy.
#
# pr-checks.yml làm đầy đủ hơn:
#   - validate-manifests (kubeconform + placeholder check)
#   - lint-and-test      (flake8 + pytest cho 3 service)
#   - dockerfile-lint    (hadolint)
#
# Branch Protection nên dùng:
#   Required status checks: "validate-manifests", "lint-and-test"
#   (từ pr-checks.yml, không phải file này)
name: "DEPRECATED — xem pr-checks.yml"
on:
  workflow_dispatch:  # chỉ chạy khi bấm tay, không tự động
jobs: {}
