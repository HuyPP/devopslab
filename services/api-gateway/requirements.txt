flask==3.0.3
prometheus-flask-exporter==0.23.1
requests==2.32.3
gunicorn==22.0.0
# test & lint (CI only — không cần trong Docker image)
# pip install -r requirements.txt thêm pytest==8.3.3 flake8==7.1.1 khi test
